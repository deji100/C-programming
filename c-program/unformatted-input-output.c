#include <stdio.h>
#include <stdlib.h>

void main() 
{
    system("cls");

    char ch[100];
    // ch = getchar();
    // ch = getch();
    // ch = getche();
    fgets(ch, 100, stdin);
    puts('a');

    // printf("Name: %c", ch);
    // printf("Name: %s", ch);
}