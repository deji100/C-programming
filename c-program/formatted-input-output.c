#include <stdio.h>
#include <conio.h>

void main() 
{
    system("cls");

    int a, b, sum, c=1234.3456;

    // scanf("Control String", arg1, arg2, argn);

    // printf("%d, %d \n", &a, &b);
    // scanf("%d %d \n", &a, &b);
    
    sum = a + b;

    // printf("Sum %d \n", sum);

    // printf("%06d", c);

    printf("%5.2f", c);

    getch();
}